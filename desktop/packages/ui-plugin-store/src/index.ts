/** Host loader entry for the Plugins-panel browser implementation exported from `./client`. */

/** Host plugin body — the panel's host half is the desktop plugin store itself. */
export function apply(): void {}
